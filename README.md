# ResumeAI Analyzer

AI-driven веб-приложение (React + Vite) для анализа резюме с режимом Recruiter Mode и премиум-шаблонами.

## ✨ Ключевые возможности

- **AI-анализ резюме**: парсинг PDF через pdf.js и оценка (score) с подсказками от OpenAI (GPT-4/4o).
- **Ключевые слова и структура**: рекомендации по улучшению контента и структурирования под IT/marketing вакансии.
- **Recruiter Mode**: шаринг анализа по ссылке без передачи персональных данных.
- **Freemium-модель**: базовый анализ бесплатный, премиум-шаблоны доступны в модальном окне (симуляция Stripe checkout).
- **PWA + iOS-like UI**: темная тема, frosted glass, плавные анимации (framer-motion), поддержка standalone-режима.

## 🚀 Старт

```bash
npm install
npm run dev
```

Открой `http://localhost:5173` в браузере.

## 🔑 Переменные окружения

Создай файл `.env.local` и добавь ключи API:

```
VITE_OPENAI_API_KEY=sk-ваш-ключ
VITE_OPENAI_IMAGE_API_KEY=sk-ваш-ключ-для-генерации-изображений
VITE_GROQ_API_KEY=gsk_ваш-ключ-groq
```

**Примечание**: Можно использовать либо OpenAI, либо Groq для анализа резюме. Groq поддерживает streaming-режим для более быстрого отображения результатов.

> ⚠️ Никогда не коммить реальные ключи. Для продакшена используй secrets в Vercel/Netlify.

## 🚢 GitHub → Vercel деплой

1. **GitHub Secrets**  
   - В репозитории открой `Settings → Secrets and variables → Actions`.  
   - Добавь секрет `VITE_GROQ_API_KEY` (и другие, если нужны).  
   - Теперь каждый push в `main` будет собирать проект с нужным ключом во время CI.

2. **Подключение к Vercel**  
   - Импортируй репозиторий на [vercel.com/import](https://vercel.com/import).  
   - В настройках проекта (`Settings → Environment Variables`) создай переменную `VITE_GROQ_API_KEY`.  
   - Значение можно синхронизировать с GitHub secret (кнопка “Import from GitHub”).  
   - Vercel автоматически запускает `npm install && npm run build`, а директория `dist/` разворачивается как статика.

3. **Локальная проверка перед пушем**  
   ```bash
   npm run lint
   npm run build
   npm run preview
   ```
   - Так ты увидишь ошибки до деплоя. Для полной симуляции Vercel-окружения можно использовать `vercel dev`, он прочитает переменные из `.env.local`.

4. **Смена ключа**  
   - Обнови секрет в GitHub и переменную в Vercel.  
   - Выполни `npm run dev -- --clearScreen=false` локально, чтобы убедиться что `Groq API Key loaded: Yes (hidden)` появляется в консоли.  
   - Сделай коммит и пуш — Vercel пересоберёт проект автоматически.

## 🧱 Структура

```
src/
  components/      Общее UI (Header, Footer, Dropzone, AnalysisCard, PremiumModal)
  pages/           Маршруты: Landing, Analyzer, Recruiter
  services/        Интеграции: OpenAI, pdf.js, shareable links
  styles/          Глобальные SCSS и переменные
  types/           Типы для анализа и окружения
```

## 🛠️ Технологии

- **React 18 + TypeScript** (Vite)
- **SCSS Modules** + глобальные токены
- **framer-motion** — анимации в стиле iOS 16
- **react-dropzone** — загрузка PDF
- **pdfjs-dist** — парсинг текста резюме внутри браузера
- **axios + OpenAI API** — анализ и генерация советов
- **react-share** — быстрый шаринг результата
- **vite-plugin-pwa** — подготовка к установке на устройства

## 📱 PWA / Deployment

- `vite-plugin-pwa` автоматически генерит сервис-воркер и манифест.
- Для Vercel/Netlify достаточно запустить `npm run build`; собранная папка `dist/` готова к деплою.
- Добавь реальные иконки (192/512 px) в манифест при продовом запуске.

## 🔐 Recruiter Mode

- Анализ сохраняется в `localStorage` (можно заменить на Firebase/Supabase).
- Шаринг осуществляется через `...?resumeId=UUID`, данные приватные.

## 🧪 Тестирование

Минимальный smoke-тест:

1. `npm run dev`
2. Перетащи тестовый `resume.pdf` → убедись, что отображается score и списки советов.
3. Сгенерируй ссылку → открой в новом окне `/recruiter?resumeId=...`.
4. Проверь Premium-модал (кнопка в карточке анализа).

## 📄 Лицензия

MIT — можно использовать и расширять по своему усмотрению.

